<Page
    x:Class="PiDeo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PiDeo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid VerticalAlignment="Center" HorizontalAlignment="Center"  Grid.Row="0">
            <Image Source="Assets/2017_logo_aldeo.png" MaxWidth="420"></Image>
        </Grid>

        <Grid Grid.Row="1">
            <StackPanel>
                <Grid>
                    <TextBlock Name="Answer"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="60" />
                    </Grid.ColumnDefinitions>
                    <TextBox Name="Message"/>
                    <Button Grid.Column="1" Content="Send" Click="ButtonBase_OnClick"></Button>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
